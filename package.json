{
  "name": "systemx",
  "version": "1.0.0",
  "description": "SystemX - Automação Discord com painel web integrado",
  "main": "dist/index.js",
  "scripts": {
    "build:frontend": "cd frontend && npm install && npm run build",
    "build:backend": "npx tsc || echo 'Ignorando erros de tipo no build do Railway'",
    "build": "npx prisma generate && npx tsc && cd frontend && npm install && npm run build",
    "start": "node dist/index.js",
    "dev": "npx ts-node-dev --respawn src/index.ts",
    "prisma:generate": "npx prisma generate",
    "prisma:migrate": "npx prisma migrate deploy",
    "postinstall": "npx prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.10.0",
    "cors": "^2.8.5",
    "discord.js-selfbot-v13": "^3.4.0",
    "dotenv": "^16.4.5",
    "express": "^4.18.3"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.19",
    "prisma": "^5.10.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
